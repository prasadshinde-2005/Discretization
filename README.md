# Discretization
Discretization (binning) is the process of converting continuous numerical data into discrete categories/bins. It simplifies data, handles outliers, and helps linear models capture non-linear patterns. Common strategies: uniform, quantile, k-means. Used via sklearn's KBinsDiscretizer.
